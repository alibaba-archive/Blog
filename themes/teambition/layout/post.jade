extends _layout

block content
  .container
    .row
      .span8.offset2
        include _article
        mixin article(page, false)
        if config.disqus_shortname
          include _social
          mixin social()
